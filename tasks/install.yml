---
  - name: Install Nginx package
    package:
      name: "{{ item }}"
      state: present
    with_items: '{{ nginx_packages }}'
    tags:
      - nginx
      - nginx-install

  - name: Make sure that the service Nginx is running
    service:
      name: nginx
      state: started
      enabled: yes
